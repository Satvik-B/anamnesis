# Minimal Memory Rule

This is a minimal example. For the full memory rule, copy from:

```
skeleton/rules/memory-rule.md
```

Or run `anamnesis init` in your project to get the complete setup automatically.
